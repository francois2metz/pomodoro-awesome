Pomodoro Widget
===============

In you rc.lua:


   // insert after require("beautiful")
   require("pomodoro")

   -- widget are now available via:
   pomodoro.pomodoro

Add it to your wibox:

    mywibox[s].widgets = {
        pomodoro.pomodoro,
        mytextclock,
    }